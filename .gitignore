# Local helper scripts + working artifacts
_*.py
!**/__init__.py
_screenshots/
.claude/
.server_pull/
*.log

# Python / agent runtime artifacts
__pycache__/
*.pyc
.env
venv/
EstimaroAgent/screenshots/
EstimaroAgent/venv/

# OS
.DS_Store
Thumbs.db

# Local deliverables (regenerate from Excel script)
Estimaro-Credentials-Master.xlsx

# Solution docs are tracked separately
# Estimaro-Solution-Proposal.pdf  (keep tracked)

# session-local — never commit (passwords / customer PII / DBs)
.handoff_context.md
EstimaroAgent/data/
*.db
